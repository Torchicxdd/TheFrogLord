class_name LevelEnemyData extends Node

# Make sure each RoundEnemyData does not contain the same values
# This oculd be improved to not work like this but not enough time
var enemies_per_level: Array[RoundEnemyData]
